# Futbol Game

## TL;DR

### Creating a venv
>create a venv
```
python -m venv venv
```
>activate venv (windows)
- just in case of permission prblems
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
- Activation
 .\venv\Scripts\Activate.ps1

### installing dependecies

pip install -r  requirements.txt 


py --list
 -V:3.13 *        Python 3.13 (64-bit)
PS C:\proyectos\games\futbol> py -3.13 -m venv venv
PS C:\proyectos\games\futbol> .\venv\Scripts\Activate.ps1
(venv) PS C:\proyectos\games\futbol> python -m pip install -r requirements.txt